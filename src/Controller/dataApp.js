/**
 * url of this APP
 * @category Constant App
 * @type {string} url App
 */
const url='http://localhost:3000'

/**
 * requestOptions for fetching
 * @category Constant App
 * @type {object} options for the request fetch
 */
const requestOptions = {
    method: 'GET',
    redirect: 'follow'
};

export { url, requestOptions}
